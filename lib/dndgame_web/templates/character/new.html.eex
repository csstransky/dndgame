<h1><u>Create your hero!</u></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.character_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.character_path(@conn, :index) %></span>
